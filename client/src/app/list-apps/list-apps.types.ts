export interface AppList {
  app: {
    algorithms: [{
      version: string,
      description: string,
      _id: string
    }],
    author: string,
    name: string,
    description: string,
    _id: string
  };
}
